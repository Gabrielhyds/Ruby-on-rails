<%= form_with(model: mining_type, class: "needs-validation", novalidate: true) do |form| %>
  <% if mining_type.errors.any? %>
    <div class="alert alert-danger">
      <h6 class="alert-heading">
        <%= pluralize(mining_type.errors.count, "error") %> prohibited this mining type from being saved:
      </h6>
      <ul class="mb-0">
        <% mining_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :description, class: "form-label" %>
    <%= form.text_field :description, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :acronym, class: "form-label" %>
    <%= form.text_field :acronym, class: "form-control" %>
  </div>

  <div>
    <%= form.submit t('buttons.save', default: "Save"), class: "btn btn-primary w-100" %>
  </div>
<% end %>
